Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports |
enrolled_rp + age_hh + age_sp + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports |
enrolled_rp + age_hh + age_sp + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports |
enrolled_rp + age_hh + age_sp + poverty_index +poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports |
enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
HISP_treatment <- HISP_treatment %>%
mutate(enrolled_dummy = ifelse(enrolled == "Enrolled", 1, 0), round_dummy = ifelse(round == "After", 1, 0))
HISP_Treatment <- HISP_Treatment %>%
mutate(enrolled_dummy = ifelse(enrolled == "Enrolled", 1, 0), round_dummy = ifelse(round == "After", 1, 0))
Hisp_Treatment <- Hisp_Treatment %>%
mutate(enrolled_dummy = ifelse(enrolled == "Enrolled", 1, 0), round_dummy = ifelse(round == "After", 1, 0))
Hisp_Treatment <- Hisp_Treatment %>%
mutate(enrolled_dummy = ifelse(enrolled == "Enrolled", 1, 0), round_dummy = ifelse(round == "After", 1, 0))
HISP_treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
Hisp_Treatment <- Hisp_Treatment %>%
mutate(enrolled_dummy = ifelse(enrolled == "Enrolled", 1, 0), round_dummy = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(enrolled_dummy = ifelse(enrolled == "Enrolled", 1, 0), round_dummy = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ after ~ enrolled_dummy + round_dummy + enrolled_dummy*round_dummy, data = Hisp_Treatment)
m3 <- lm(health_expenditures ~ after ~ enrolled_dummy + round_dummy + enrolled_dummy*round_dummy, data = Hisp_Treatment)
m3 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = HISP_treatment)
m3 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
summary(m3)
m3 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy*round_dummy, data = Hisp_Treatment)
summary(m3)
m3 <- lm(health_expenditures ~ test1 + round_dummy + test1*round_dummy, data = Hisp_Treatment)
m3 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy*round_dummy, data = Hisp_Treatment)
summary(m3)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + round + enrolled_dummy*round_dummy, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + round + is_enrolled*round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + round + is_enrolled*round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
print(round)
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + round + is_enrolled*round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
round
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + round + is_enrolled*round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + the_round + is_enrolled*the_round, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), the_round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + the_round + is_enrolled*the_round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
m3 <- lm(health_expenditures ~ enrolled + round + enrolled*round, data = traitement)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), the_round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ enrolled + round + enrolled*round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
library(tidyverse)   # ggplot(), %>%, mutate(), and friends
library(scales)      # Format numbers with functions like comma(), percent(), and dollar()
library(broom)       # Convert models to data frames
library(stargazer)   # Makes nice tables
library(lmtest)      # To test linear regression models
library(AER)         # Applied econometrics in R
library(rdrobust)    # For robust nonparametric regression discontinuity
library(rddensity)   # For nonparametric regression discontinuity density tests
Hisp = read.csv(file = 'HISP.csv') #Create a dataset based on HISP
Hisp_After = filter(Hisp, round == "After") #Create a dataset that only includes observations from after
Hisp_After
m1<- lm(health_expenditures ~ enrolled_rp, data = Hisp_After)
summary(m1)
m2 <- lm(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(m2)
fs1 <- lm(educ_hh ~ poverty_index, data = Hisp_After)
summary(fs1)
fs2 <- lm(educ_sp ~ poverty_index, data = Hisp_After)
summary(fs2)
ivmodel <- ivreg(health_expenditures ~ enrolled_rp + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports | enrolled_rp + age_hh + age_sp + poverty_index + poverty_index + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_After)
summary(ivmodel)
Hisp_Treatment = filter(Hisp, treatment_locality == "Treatment") #Make a new dataset based on HISP that only includes observations from the localities that were randomly chosen for treatment
Hisp_Treatment
Hisp_Treatment <- Hisp_Treatment %>%
mutate(is_enrolled = ifelse(enrolled == "Enrolled", 1, 0), the_round = ifelse(round == "After", 1, 0))
Hisp_Treatment %>%
group_by(round,enrolled) %>%
summarise(mean(health_expenditures), mean(age_hh), mean(age_sp), mean(educ_hh), mean(educ_sp), mean(female_hh), mean(indigenous), mean(hhsize), mean(dirtfloor), mean(bathroom), mean(land), mean(land), mean(hospital_distance), mean(park), mean(sports))
m3 <- lm(health_expenditures ~ is_enrolled + the_round + is_enrolled*the_round, data = Hisp_Treatment)
summary(m3)
m4 <- lm(health_expenditures ~ enrolled_dummy + round_dummy + enrolled_dummy * round_dummy + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
is_enrolled
print(is_enrolled)
m4 <- lm(health_expenditures ~ is_enrolled + the_round + is_enrolled * the_round + age_hh + age_sp + educ_hh + educ_sp + female_hh + indigenous + hhsize + dirtfloor + bathroom + land + hospital_distance + park + sports, data = Hisp_Treatment)
summary(m4)
method = c('OLS', 'OLS', 'IV', 'Diff-in-Diff', 'Diff-in-Diff')
including_control_variable = c('no', 'yes', 'yes', 'no', 'yes')
estimate = c(m1(ols$coefficients), m2(ols1$coefficients), ivmodel(iv$coefficients), m3(did$coefficients), m4(did1$coefficients))
method = c('OLS', 'OLS', 'IV', 'Diff-in-Diff', 'Diff-in-Diff')
including_control_variable = c('no', 'yes', 'yes', 'no', 'yes')
estimate = c(toString(m1$coefficients), toString(m2$coefficients), toString(ivmodel$coefficients), toString(m3$coefficients), toString(m4$coefficients))
cbind.data.frame(method, including_control_variable, estimate)
method = c('OLS', 'OLS', 'IV', 'Diff-in-Diff', 'Diff-in-Diff')
including_control_variable = c('no', 'yes', 'yes', 'no', 'yes')
estimate = c(toString(m1$coefficients), toString(m2$coefficients), toString(ivmodel$coefficients), toString(m3$coefficients), toString(m4$coefficients))
cbind.data.frame(method, including_control_variable, estimate)
method = c('OLS', 'OLS', 'IV', 'Diff-in-Diff', 'Diff-in-Diff')
including_control_variable = c('no', 'yes', 'yes', 'no', 'yes')
estimate = c(toString(m1$coefficients[2]), toString(m2$coefficients[2]), toString(ivmodel$coefficients[2]), toString(m3$coefficients[4]), toString(m4$coefficients[17]))
cbind.data.frame(method, including_control_variable, estimate)
method = c('OLS', 'OLS', 'IV', 'Diff-in-Diff', 'Diff-in-Diff')
including_control_variable = c('no', 'yes', 'yes', 'no', 'yes')
estimate = c(m1$coefficients[2],m2$coefficients[2],ivmodel$coefficients[2],m3$coefficients[4],m4$coefficients[17])
cbind.data.frame(method, including_control_variable, estimate)
